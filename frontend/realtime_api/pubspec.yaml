name: realtime_api
description: The Flutter realtimeDB API.
version: 0.0.1
homepage:

environment:
  sdk: '>=3.0.0-366.0.dev <4.0.0'
  flutter: ">=1.17.0"

dependencies:
  flutter:
    sdk: flutter
  http: ^0.13.6
  web_socket_channel: ^2.4.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^2.0.0

flutter:

